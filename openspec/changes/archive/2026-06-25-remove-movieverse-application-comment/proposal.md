## Why

The `MovieVerseApplication.java` file contains a stray comment (`// tests`) that is not part of application behavior and should be removed for codebase cleanliness.

## What Changes

- Remove the `// tests` comment from `movieverse/src/main/java/com/movieverse/MovieVerseApplication.java`.
- Add a small OpenSpec specification that records the cleanup change.
- Document the change in the design and implementation task artifacts.

## Capabilities

### New Capabilities
- `remove-movieverse-application-comment`: Tracks the non-functional cleanup of the MovieVerse entrypoint class comment.

### Modified Capabilities
- None

## Impact

- Affected file: `movieverse/movieverse/src/main/java/com/movieverse/MovieVerseApplication.java`
- No runtime behavior change expected.
- No external APIs or runtime dependencies are affected.
